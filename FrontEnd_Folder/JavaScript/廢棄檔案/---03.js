/*
var box;
alert(typeof box);// box是Ｕndefined的類型,因為沒有初始化,值是undefined,類型返回的字符串是undefined

var box = true;
alert(typeof box);// box是Boolean的類型,值是true,類型返回的字符串是boolean

var box = '江宛婷';
alert(typeof box);// box是String的類型,值是'江宛婷',類型返回的字符串是string

var box = 250;
alert(typeof box);// box是Number的類型,值是250,類型返回的字符串是number


// 大括號裡面 空的對象：表示這個對象創建了,裡面沒東西
// 空對象：表示沒有創建,是一個null
var box = {};
alert(typeof box);// box是Object的類型,值是[object Object],類型返回的字符串是object


var box =null;
alert(typeof box);// box是Null的類型,值是null,類型返回的字符串是object


var box = new Object();
alert(box);// box是Object的類型,值是[object Object],類型返回的字符串是object


var box = new Object();
alert(box);// box是Object的類型,值是[object Object],類型返回的字符串是object

function box(){

};
alert(typeof box);// box是function,值是function box(){},類型返回的字符串是function

*/ 





